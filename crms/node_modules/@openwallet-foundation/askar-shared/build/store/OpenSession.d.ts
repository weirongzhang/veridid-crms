import type { StoreHandle } from '../crypto';
import { Session } from './Session';
export declare class OpenSession {
    private store;
    private profile?;
    private isTxn;
    private session?;
    constructor({ store, isTxn, profile }: {
        store: StoreHandle;
        profile?: string;
        isTxn: boolean;
    });
    open(): Promise<Session>;
}
