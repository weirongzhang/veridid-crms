export type AskarErrorObject = {
    code: number;
    message: string | null;
};
export declare class AskarError extends Error {
    readonly code: number;
    constructor({ code, message }: AskarErrorObject);
    static customError({ message }: {
        message: string;
    }): AskarError;
}
export declare function handleInvalidNullResponse<T extends null | unknown>(response: T): Exclude<T, null>;
