import { AnoncredsObject } from '../AnoncredsObject';
import type { JsonObject } from '../types';
import { CredentialDefinition } from './CredentialDefinition';
import { CredentialOffer } from './CredentialOffer';
import { CredentialRequestMetadata } from './CredentialRequestMetadata';
export type CreateCredentialRequestOptions = {
    entropy?: string;
    proverDid?: string;
    credentialDefinition: CredentialDefinition | JsonObject;
    linkSecret: string;
    linkSecretId: string;
    credentialOffer: CredentialOffer | JsonObject;
};
export declare class CredentialRequest extends AnoncredsObject {
    static create(options: CreateCredentialRequestOptions): {
        credentialRequest: CredentialRequest;
        credentialRequestMetadata: CredentialRequestMetadata;
    };
    static fromJson(json: JsonObject): CredentialRequest;
}
