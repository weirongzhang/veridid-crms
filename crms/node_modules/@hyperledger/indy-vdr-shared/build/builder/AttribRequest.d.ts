import { IndyVdrRequest } from '../indyVdr';
import type { WriteRequestResponse, WriteRequestResultTxnBase } from '../types';
export type AttribRequestOptions = {
    submitterDid: string;
    targetDid: string;
    hash?: string;
    raw?: string;
    enc?: string;
};
interface AttribResultTxn extends WriteRequestResultTxnBase {
    type: '100';
    data: {
        raw?: string;
        enc?: string;
        hash?: string;
        dest: string;
    };
}
export type AttribResponse = WriteRequestResponse<AttribResultTxn>;
export declare class AttribRequest extends IndyVdrRequest<AttribResponse> {
    constructor(options: AttribRequestOptions);
}
export {};
