import { IndyVdrRequest } from '../indyVdr';
import type { WriteRequestResponse, WriteRequestResultTxnBase } from '../types';
export type SchemaRequestOptions = {
    submitterDid: string;
    schema: {
        id: string;
        name: string;
        version: string;
        attrNames: string[];
        seqNo?: number;
        ver: '1.0';
    };
};
interface SchemaResultTxn extends WriteRequestResultTxnBase {
    type: '101';
    data: {
        data: {
            version: string;
            attr_names: string[];
            name: string;
        };
    };
}
export type SchemaResponse = WriteRequestResponse<SchemaResultTxn>;
export declare class SchemaRequest extends IndyVdrRequest<SchemaResponse> {
    constructor(options: SchemaRequestOptions);
}
export {};
