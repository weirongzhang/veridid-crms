import { IndyVdrPool } from '../indyVdr';
export type PoolCreateOptions = {
    parameters: {
        transactions?: string;
        transactions_path?: string;
        node_weights?: Record<string, number>;
    };
};
export declare class PoolCreate extends IndyVdrPool {
    constructor(options: PoolCreateOptions);
}
