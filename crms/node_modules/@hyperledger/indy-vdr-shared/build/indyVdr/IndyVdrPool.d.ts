import type { PoolStatus, Transactions, Verifiers } from '../types';
import type { IndyVdrRequest, RequestResponseType } from './IndyVdrRequest';
interface SubmitActionOptions {
    nodes?: string[];
    timeout?: number;
}
export declare class IndyVdrPool {
    private _handle;
    constructor(options: {
        handle: number;
    });
    get handle(): number;
    get status(): Promise<PoolStatus>;
    get verifiers(): Promise<Verifiers>;
    get transactions(): Promise<Transactions>;
    refresh(): Promise<void>;
    close(): void;
    submitAction<Request extends IndyVdrRequest>(request: Request, options?: SubmitActionOptions): Promise<RequestResponseType<Request>>;
    submitRequest<Request extends IndyVdrRequest>(request: Request): Promise<RequestResponseType<Request>>;
}
export {};
