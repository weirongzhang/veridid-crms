export type IndyVdrErrorObject = {
    code: number;
    extra?: string;
    message: string;
};
export declare class IndyVdrError extends Error {
    readonly code: number;
    readonly extra?: string;
    constructor({ code, message, extra }: IndyVdrErrorObject);
    static customError({ message }: {
        message: string;
    }): IndyVdrError;
}
export declare function handleInvalidNullResponse<T extends null | unknown>(response: T): Exclude<T, null>;
