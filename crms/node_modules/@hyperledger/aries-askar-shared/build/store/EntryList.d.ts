import type { EntryObject } from '.';
import type { EntryListHandle } from '../crypto';
import { Entry } from './Entry';
export declare class EntryList {
    private _handle;
    private _length;
    constructor({ handle, length }: {
        handle: EntryListHandle;
        length?: number;
    });
    get handle(): EntryListHandle;
    get length(): number;
    getEntryByIndex(index: number): Entry;
    private forEach;
    find(cb: (entry: Entry, index?: number) => boolean): Entry | undefined;
    toArray(valuesAreJson?: boolean): EntryObject[];
}
